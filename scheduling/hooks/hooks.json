{
  "description": "Scheduling — advisory hook steering Claude toward the parse/generate scripts for managed HTML artifacts (project-context.html). Always advisory; never blocks. The wrapper (run-hook.ps1) blocks (exit 2) if no real Python is on PATH so silent failure modes are surfaced.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"$r=$env:CLAUDE_PLUGIN_ROOT;if($r -match '^/([a-zA-Z])/(.*)$'){$r=$matches[1].ToUpper()+':/'+$matches[2]};& \\\"$r/hooks/run-hook.ps1\\\" check_html_discipline.py;exit $LASTEXITCODE\""
          }
        ]
      }
    ]
  }
}
